const CACHE='alexandra-lucas-v2-2-logo-final';
const ASSETS=['./','./index.html','./styles.css?v=2.2','./v22.css?v=2.2','./app-v22-loader.js?v=2.2','./app-v22.payload?v=2.2','./logo-officiel.txt?v=2.2','./config.js?v=2.2','./manifest.webmanifest?v=2.2'];
async function officialLogoResponse(){const text=await fetch('./logo-officiel.txt?v=2.2',{cache:'no-store'}).then(r=>r.text());const clean=text.replace(/\s/g,'');const binary=atob(clean);const bytes=Uint8Array.from(binary,c=>c.charCodeAt(0));return new Response(bytes,{headers:{'Content-Type':'image/png','Cache-Control':'public, max-age=31536000'}})}
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(ASSETS)).then(()=>self.skipWaiting())));
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const url=new URL(event.request.url);if(['icon-180.png','icon-192.png','icon-512.png','logo-officiel.png'].some(name=>url.pathname.endsWith('/'+name))){event.respondWith(officialLogoResponse());return}event.respondWith(fetch(event.request).then(response=>{const copy=response.clone();caches.open(CACHE).then(cache=>cache.put(event.request,copy));return response}).catch(()=>caches.match(event.request).then(cached=>cached||caches.match('./index.html'))))});
